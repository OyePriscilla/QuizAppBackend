services:
  - type: web
    name: bible-quiz-backend
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      # Add more like DB_URL, JWT_SECRET etc.
